<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
  <nav  class="navbar navbar-expand-lg ">
    <div class="container-fluid">
         <!-- Navbar Toggle Button (for Mobile) -->
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
            <span class="navbar-toggler-icon"></span>
        </button>

        
        <div class="collapse navbar-collapse justify-content-between" id="navbarNav">
            <!-- Left Empty Space for Center Alignment -->
            <div class="flex-grow-1"></div>
            
          
            <a class="navbar-brand ms-auto" href="/products">
                <img src="https://encrypted-tbn0.gstatic.com/images?q=tbn:ANd9GcTdqfrtvanWDhhAhe07PgTLwQDe2fiAohmyp1GKuVJbzC0wZr6zBDgQv5ZIYnmpfeBpqYI&usqp=CAU" 
                     alt="Logo" class="d-inline-block align-text-top rounded" 
                     style="width: 150px; height: 55px; object-fit: cover; margin-left: -1462px; ">
            </a>
        </div>
    </div>
</nav>
<% layout('layouts/boilerplate') -%>
<div class="container mt-4">
    <h2 style="color: black;" class="text-center mb-4">Search Results for "<%= query %>"</h2>
    
    <% if (products.length === 0) { %>
        <h4 class="text-center text-muted">No products found</h4>
    <% } else { %>
        <div class="row row-cols-1 row-cols-sm-2 row-cols-md-3 row-cols-lg-4 g-4">
            <% products.forEach(product => { %>
                <div class="col">
                    <div class="card h-100 shadow-sm border-0">
                        <img src="<%= product.img %>" class="card-img-top p-2" alt="<%= product.name %>" style="height: 350px; object-fit: fill;">
                        <div class="card-body text-center">
                            <h5 class="card-title fw-bold text-truncate"><%= product.name %></h5>
                            <p class="text-danger fw-semibold fs-5">₹<%= product.price %></p>
                            <a href="/products/<%= product._id %>" class="btn btn w-100">View Product</a>
                        </div>
                    </div>
                </div>
            <% }) %>
        </div>
    <% } %>
</div>

